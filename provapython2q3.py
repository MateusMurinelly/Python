print('seja bem vindo')

print('vamos calcular a área do retângulo usando a base e a altura ')

base = float(input('digite o valor da base: '))
altura = float(input('digite o valor da altura: '))
area = base * altura
print(f'A tendo altura {altura} e base {base} concluimos que a área é de {area}')